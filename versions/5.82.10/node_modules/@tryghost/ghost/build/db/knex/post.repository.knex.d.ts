import ObjectID from 'bson-objectid';
import { PostRepository } from '../../core/activitypub/post.repository';
type UrlUtils = {
    transformReadyToAbsolute(html: string): string;
};
export declare class KnexPostRepository implements PostRepository {
    private readonly knex;
    private readonly urlUtils;
    constructor(knex: any, urlUtils: UrlUtils);
    getOne(identifier: ObjectID): Promise<{
        id: ObjectID;
        title: any;
        html: string;
        slug: any;
        visibility: any;
    } | null>;
    getOneById(id: ObjectID): Promise<{
        id: ObjectID;
        title: any;
        html: string;
        slug: any;
        visibility: any;
    } | null>;
}
export {};
