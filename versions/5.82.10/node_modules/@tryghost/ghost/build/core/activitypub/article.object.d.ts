/// <reference types="node" />
import ObjectID from 'bson-objectid';
import { ActivityPub } from './types';
import { Post } from './post.repository';
type ArticleData = {
    id: ObjectID;
    name: string;
    content: string;
    url: URL;
};
export declare class Article {
    private readonly attr;
    constructor(attr: ArticleData);
    getJSONLD(url: URL): ActivityPub.Article & ActivityPub.RootObject;
    static fromPost(post: Post): Article;
}
export {};
