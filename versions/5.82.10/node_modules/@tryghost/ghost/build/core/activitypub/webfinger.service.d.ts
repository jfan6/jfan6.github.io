/// <reference types="node" />
import { ActorRepository } from './actor.repository';
export declare class WebFingerService {
    private repository;
    private url;
    constructor(repository: ActorRepository, url: URL);
    getResource(resource: string, rel?: string[]): Promise<{
        subject: string;
        links: {
            rel: string;
            type: string;
            href: string;
        }[];
    }>;
}
