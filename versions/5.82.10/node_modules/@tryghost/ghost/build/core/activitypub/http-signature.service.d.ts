/// <reference types="node" />
/// <reference types="node" />
/// <reference types="node" />
/// <reference types="node" />
import crypto from 'node:crypto';
export declare class HTTPSignature {
    private static generateSignatureString;
    private static parseSignatureHeader;
    private static getHeader;
    protected static getPublicKey(keyId: URL): Promise<crypto.KeyObject>;
    private static validateDigest;
    static validate(requestMethod: string, requestUrl: string, requestHeaders: Headers, requestBody?: Buffer): Promise<boolean>;
    static sign(request: Request, keyId: URL, privateKey: crypto.KeyObject): Promise<Request>;
}
