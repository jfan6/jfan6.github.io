import { WebFingerService } from '../../../core/activitypub/webfinger.service';
export declare class WebFingerController {
    private readonly service;
    constructor(service: WebFingerService);
    getResource(resource: unknown): Promise<{
        subject: string;
        links: {
            rel: string;
            type: string;
            href: string;
        }[];
    }>;
}
